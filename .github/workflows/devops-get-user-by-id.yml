name: Docker Image CI/CD for Get User By Id Service TEST 1

on:
  push:
    paths:
      - 'Usersmanagement/get-user-by-id/**'
      - '.github/workflows/devops-get-user-by-id.yml'
    branches: [ "dev" ]
  pull_request:
    paths:
      - 'Usersmanagement/get-user-by-id/**'
      - '.github/workflows/devops-get-user-by-id.yml'
    branches:
      - "QA"
      - "main"

jobs:
  build-and-deploy:
    name: Build, Push Docker Image and Deploy
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: usersmanagement-get-user-by-id

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        run: |
          docker build Usersmanagement/get-user-by-id/. --file Usersmanagement/get-user-by-id/Dockerfile \
            --tag ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.run_number }} \
            --tag ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest
          
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.run_number }}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          port: 22
          script: |
            set -e

            echo "==> Installing Docker (if needed)"
            if ! command -v docker &> /dev/null; then
              sudo apt-get update -y
              sudo apt-get install -y docker.io
              sudo systemctl start docker
              sudo usermod -aG docker $USER
            fi

            echo "==> Creating environment file"
            echo "APP_PORT=4000" > ~/get-user-by-id.env
            echo "PG_HOST=${{ secrets.DB_HOST }}" >> ~/get-user-by-id.env
            echo "PG_USER=${{ secrets.DB_USER }}" >> ~/get-user-by-id.env
            echo "PG_PASSWORD=${{ secrets.DB_PASSWORD }}" >> ~/get-user-by-id.env
            echo "PG_DATABASE=${{ secrets.DB_NAME }}" >> ~/get-user-by-id.env
            echo "PG_PORT=${{ secrets.DB_PORT }}" >> ~/get-user-by-id.env
            echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> ~/get-user-by-id.env
            echo "PORT=4000" >> ~/get-user-by-id.env

            echo "==> Pulling Docker image"
            sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest

            echo "==> Stopping old container (if exists)"
            if sudo docker ps -aq -f name=${{ env.IMAGE_NAME }}; then
              sudo docker stop ${{ env.IMAGE_NAME }} || true
              sudo docker rm ${{ env.IMAGE_NAME }} || true
            else
              echo "No previous container to stop or remove"
            fi

            echo "==> Running new container on port 3001"
            sudo docker run -d --name ${{ env.IMAGE_NAME }} -p 3003:4000 \
              --env-file ~/get-user-by-id.env \
              ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest

            echo "==> Verifying container startup"
            if sudo docker ps -f name=${{ env.IMAGE_NAME }} | grep ${{ env.IMAGE_NAME }}; then
              echo "✅ Deployment successful"
              rm -f ~/get-user-by-id.env
            else
              echo "❌ Deployment failed"
              exit 1
            fi
